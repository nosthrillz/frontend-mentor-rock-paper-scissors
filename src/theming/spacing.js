const defaultSpace = 2;
const defaultUnit = "rem";

export const SIZES = {
  default: defaultSpace + defaultUnit,
  small: defaultSpace * 0.5 + defaultUnit,
  medium: defaultSpace * 0.75 + defaultUnit,
  large: defaultSpace * 1.5 + defaultUnit,
};
